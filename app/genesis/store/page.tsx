import { GenesisStoreClient } from './genesis-store-client'

export const metadata = {
  title: 'Genesis Store | SageSpace',
  description: 'Unlock premium companions, achievements, and quests'
}

export default function GenesisStorePage() {
  return <GenesisStoreClient />
}
